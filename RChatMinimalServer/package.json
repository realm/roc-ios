{
    "name": "minimal-rchat-server",
    "version": "0.0.1",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "https://github.com/realm/roc-ios.git"
    },
    "keywords": ["realm", "chat"],
    "author": "David Spector, ds@realm.io",  
    "description": "A minimal server for RChat under ROS2.0",
    "main": "src/index.js",
    "scripts": {
        "build": "rm -rf dist; ./node_modules/.bin/tsc",
        "clean": "rm -rf dist",
        "start": "npm run build && node dist/index.js"
    },
    "devDependencies": {
        "typescript": "2.5.3"
    },
    "dependencies": {
        "realm-object-server": "2.0.13"
    }
}
